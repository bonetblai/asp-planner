all:
	@echo "**Making all in `pwd`"

clean:
	@echo "**Making clean in `pwd`"
	rm -f *~ relparser funparser

install:
	@echo "**Making install in `pwd`"

dist:
	@echo "**Making dist in `pwd`"
	make clean
